{
  "name": "redux-resx-feathers-middleware",
  "version": "0.2.3",
  "description": "Feathers middleware for redux-resx",
  "main": "index.js",
  "author": "Stan Bondi <stan@fixate.it>",
  "repository": "https://github.com/fixate/redux-resx-feathers-middleware",
  "license": "MIT",
  "scripts": {
    "compile": "babel --presets es2015,stage-0 -d . src/",
    "prepublish": "npm run compile",
    "watch": "watch -p '**/*.js' -c 'npm run compile'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm *.js"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "babel-preset-stage-0": "^6.22.0",
    "watch-cli": "^0.2.1"
  },
  "peerDependencies": {
    "redux-resx": "1.x"
  }
}
